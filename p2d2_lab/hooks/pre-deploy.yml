######################################################################
## Create the training user, pwd in args.yml
######################################################################

- name: create the training user
  user:
    name: user
    groups: "sudo"
    password: "{{ userpwd | password_hash('sha256') }}"
    shell: /bin/bash
